// Fixture: intentional SAST violations for unit tests (not executed)
export function dangerous(userInput: string): void {
  eval(userInput);
  const fn = new Function('return ' + userInput);
  document.write(userInput);
  (globalThis as { el?: { innerHTML: string } }).el!.innerHTML = userInput;
  execSync(`rm -rf ${userInput}`);
  const api_key = 'sk_live_hardcoded_secret_value_12345';
}
